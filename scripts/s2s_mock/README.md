# s2s_mock — a turn-based stand-in for the cascaded pipeline

Same three models as `speech-to-speech --mode websocket`, same command line,
none of the streaming. It exists because the real server is built for live
conversation and a benchmark is not one.
